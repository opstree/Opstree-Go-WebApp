input {
  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{indexname}-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "elastic"
  }
}
